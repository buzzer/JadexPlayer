<!--
	<H3>Test agent creation performance.</H3>

	Creates a number of peers and prints out the time/memory it takes.
-->
<agent xmlns="http://jadex.sourceforge.net/jadex"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jadex.sourceforge.net/jadex
	                    http://jadex.sourceforge.net/jadex-0.96.xsd"
	name="AgentCreation"
	package="jadex.benchmarks">

	<imports>
		<import>jadex.planlib.*</import>
	</imports>

	<capabilities>
		<capability name="amscap" file="jadex.planlib.AMS"/>
	</capabilities>

	<beliefs>
		<belief name="delay" class="int">
			<fact>0</fact>
		</belief>
		<belief name="max" class="Integer" exported="true">
			<fact>new Integer(1000)</fact>
		</belief>
		<belief name="num" class="int" exported="true">
			<fact>new Integer(1)</fact>
		</belief>
		<belief name="starttime" class="Long" exported="true">
			<fact>new Long(System.currentTimeMillis())</fact>
		</belief>
		<belief name="startmem" class="Long" exported="true">
			<fact>new Long(Runtime.getRuntime().totalMemory()-Runtime.getRuntime().freeMemory())</fact>
		</belief>
	</beliefs>

	<goals>
		<achievegoalref name="ams_create_agent">
			<concrete ref="amscap.ams_create_agent"/>
		</achievegoalref>
		<achievegoalref name="ams_destroy_agent">
			<concrete ref="amscap.ams_destroy_agent"/>
		</achievegoalref>
	</goals>

	<plans>
		<plan name="start_peer">
			<body class="StartPeerPlan" />
			<!-- <body>new StartPeerPlan()</body> -->
		</plan>
	</plans>
	
	<properties>
		<property name="logging.level">java.util.logging.Level.WARNING</property>
	</properties>

	<configurations>
		<configuration name="default">
			<plans>
				<initialplan ref="start_peer"/>
			</plans>
		</configuration>
		<configuration name="slow (for debugging)">
			<beliefs>
				<initialbelief ref="delay">
					<fact>300</fact>
				</initialbelief>
			</beliefs>
			<plans>
				<initialplan ref="start_peer"/>
			</plans>
		</configuration>
	</configurations>
</agent>

