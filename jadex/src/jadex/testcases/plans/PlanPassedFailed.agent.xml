<!--
	<H3>Test the plans passed() and failed() methods.</H3>
	
	Test, if the plan methods passed() and failed() are
	called properly, according to the plan behaviour.<br><br>

	The agent create 4 goal in sequence and always
	uses the PassedFailedPlan to execute a further
	testcase. The belief cnt captures the testcase no.

	aborted() method is tested in PlanAbort.agent (abort on failure)
	and PlanContext.agent (abort on success)
-->

<agent xmlns="http://jadex.sourceforge.net/jadex"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jadex.sourceforge.net/jadex 
	                    http://jadex.sourceforge.net/jadex-0.96.xsd"
	name="PlanPassedFailed"
	package="jadex.testcases.plans">

	<imports>
		<import>jadex.planlib.TestReport</import>
	</imports>

	<capabilities>
		<capability name="testcap" file="jadex.planlib.Test"/>
	</capabilities>

	<beliefs>
		<beliefsetref name="reports" class="TestReport">
			<concrete ref="testcap.reports"/>
		</beliefsetref>
		<beliefref name="testcase_cnt" class="int">
			<concrete ref="testcap.testcase_cnt"/>
		</beliefref>
		<beliefref name="timeout" class="long" exported="true">
			<concrete ref="testcap.timeout"/>
		</beliefref>
		<beliefref name="testcenter" class="jadex.adapter.fipa.AgentIdentifier" exported="true">
			<concrete ref="testcap.testcenter"/>
		</beliefref>

		<belief name="cnt" class="int">
			<fact>0</fact>
		</belief>
	</beliefs>

	<goals>
		<achievegoal name="init" retry="false">
			<creationcondition>$beliefbase.cnt&lt;4</creationcondition>
		</achievegoal>
	</goals>

	<plans>
        <plan name="passfail">
			<body>new PassedFailedPlan()</body>
			<trigger>
				<goal ref="init"/>
			</trigger>
		</plan>
	</plans>

	<properties>
		<!-- <property name="logging.level">java.util.logging.Level.INFO</property> -->
        <!-- Reduce level from severe to warning, as exception is part of the test. -->
        <property name="logging.level.exceptions">java.util.logging.Level.WARNING</property>
    </properties>

	<configurations>
		<configuration name="default">
			<beliefs>
				<initialbelief ref="testcase_cnt">
					<fact>4</fact>
				</initialbelief>
			</beliefs>
		</configuration>
	</configurations>

</agent>

