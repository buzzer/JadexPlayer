<!--
	<H3>Test an inline plan.</H3>

	<i>Note: This test does not work without the expression
	compiler add-on.</i><br><br>
	Inline plans have an inline plan body, which means
	that they directly contain the body code.
	Inline plans should be used for simple plans only.
-->

<agent xmlns="http://jadex.sourceforge.net/jadex"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jadex.sourceforge.net/jadex
	                    http://jadex.sourceforge.net/jadex-0.96.xsd"
	name="InlinePlan"
	package="jadex.testcases.plans">

	<imports>
		<import>jadex.planlib.*</import>
	</imports>

	<capabilities>
		<capability name="testcap" file="jadex.planlib.Test"/>
	</capabilities>

	<beliefs>
		<beliefsetref name="reports" class="TestReport">
			<concrete ref="testcap.reports"/>
		</beliefsetref>
		<beliefref name="timeout" class="long" exported="true">
			<concrete ref="testcap.timeout"/>
		</beliefref>
		<beliefref name="testcenter" class="jadex.adapter.fipa.AgentIdentifier" exported="true">
			<concrete ref="testcap.testcenter"/>
		</beliefref>
	</beliefs>
	
	<goals>
		<achievegoal name="testgoal"/>
	</goals>

	<plans>
		<plan name="inline">
			<body inline="true">
 				getLogger().info("Inline plan started: "+this);
				waitFor(100);
				getLogger().info("Inline plan end: "+this);
				<passed>getLogger().info("Passed: "+this);</passed>
				<failed>getLogger().info("Failed: "+this);</failed>
				<aborted>getLogger().info("Aborted: "+this);</aborted>
			</body>
			<trigger><goal ref="testgoal"/></trigger>
		</plan>
		<plan name="testinline">
			<body>new TestInlinePlan()</body>
		</plan>
	</plans>

	<configurations>
		<configuration name="default">
			<plans>
				<initialplan ref="testinline"/>
			</plans>
		</configuration>
	</configurations>

</agent>

