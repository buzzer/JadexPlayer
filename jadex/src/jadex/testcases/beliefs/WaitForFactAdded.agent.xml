<!--
	<H3>Test detection of added facts.</H3>

	Testing the waitqueue using addBeliefSet() to wait for
	fact added events. This testcase currently fails as the
	condition_triggered events cannot be associated with the
	waitFor() filter. This is because the filter checks if the
	event originated from the same source condition (but here
	we have 2 conditions, one in the waitqueue and one in the 
	waitfor).	
-->

<agent xmlns="http://jadex.sourceforge.net/jadex"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://jadex.sourceforge.net/jadex 
	                    http://jadex.sourceforge.net/jadex-0.96.xsd"
	name="WaitForFactAdded"
	package="jadex.testcases.beliefs">

	<imports>
		<import>java.text.*</import>
		<import>java.util.Date</import>
	</imports>

	<capabilities>
		<capability name="testcap" file="jadex.planlib.Test"/>
	</capabilities>

	<beliefs>
		<beliefsetref name="reports">
			<concrete ref="testcap.reports"/>
		</beliefsetref>
		<beliefref name="timeout" class="long" exported="true">
			<concrete ref="testcap.timeout"/>
		</beliefref>
		<beliefref name="testcenter" class="jadex.adapter.fipa.AgentIdentifier" exported="true">
			<concrete ref="testcap.testcenter"/>
		</beliefref>

		<beliefset class="String" name="beliefSetToAddFacts" />
	</beliefs>

	<plans>
		<plan name="tester">
			<body class="WaitForFactAddedPlan"/>
			<!-- <body>new WaitForFactAddedPlan()</body> -->
		</plan>
		<plan name="factAdder">
			<parameter class="String" name="instance" />
			<body class="FactAdderPlan"/>
			<!-- <body>new FactAdderPlan()</body> -->
		</plan>
	</plans>

	<properties>
        <!--<property name="debugging">true</property>-->
        <!--<property name="logging.level">java.util.logging.Level.FINE</property>-->
        <property name="logging.useParentHandlers">true</property>
    </properties>

	<configurations>
		<configuration name="default">
			<plans>
				<initialplan ref="tester"/>
				<initialplan ref="factAdder">
 					<parameter ref="instance">
						 <value>"A"</value>
 					</parameter>
 				</initialplan>
				<!-- <initialplan ref="factAdder">
 					<parameter ref="instance">
						 <value>"B"</value>
 					</parameter>
				</initialplan> -->
			</plans>
		</configuration>
	</configurations>

</agent>
